**结论：这是一个已成形的单研究绘图原型，但还不能保证换一套数据、换一个 agent 后仍输出科学可靠、风格一致的成稿。应先修数据契约、静默错误和规则冲突，再重写 SKILL.md；不需要恢复 200 篇论文调研。**

本次未修改文件。已阅读指定文件、查看两张 PNG、抽查四组各一份 Epochs 的元信息及缓存结构，并做了不写文件的合成数据检查；未重算整套 EEG 数据。

值得保留的是：代码先做被试内条件平均，再等权平均被试；ROI 内先平均电极，再计算被试间 SEM；头皮图使用对应组×条件、对应时间窗；每张图共享对称色标。这些主体计算是合理的。当前图也没有添加显著性标记，符合你要求的描述性展示。

以下按严重程度排序；“复用风险”不等于当前四组图已经算错。

1. **P0｜时间轴与通道契约缺失，可能把不同时刻的数据平均在一起。**

   **证据：**[erp_plot.py:71](C:/dev/brain-plot/erp-figure/erp_plot.py:71) 采用第一份文件的 `info`；第 81 行按其通道名单取数；第 85 行却采用最后一名被试的 `times`。合成检查中，两份数据分别覆盖 −100–200 ms 和 0–300 ms，样本数相同，脚本接受并按数组位置平均，最终标成 0–300 ms。

   **影响：**这会静默破坏 ERP 的潜伏期和时间窗含义。类似地，不同参考、基线、通道类型或空间坐标可能被混用。当前环境的 MNE `average()` 还会保留标记为坏道的 EEG 通道，不能把“调用 MNE”理解为已经处理坏道。

   **具体修复：**加载时逐文件验证完整时间网格、通道名称与顺序、类型、单位、基线及处理状态；不兼容就报出文件名并停止。第一版可明确只接受已完成预处理的 EEG 电位数据，要求坏道处理已解决。头皮图使用经确认的共同 montage，不能默认第一名被试的坐标就是所有人的公共坐标。**不要在绘图阶段自动重参考、插值或重采样。**

2. **P0｜缓存不能判断输入数据是否变化，可能用旧数据配新元信息。**

   **证据：**[erp_plot.py:69](C:/dev/brain-plot/erp-figure/erp_plot.py:69) 的缓存键只包含路径、组名、条件、查询和排除项，没有实际文件清单、文件版本或加载逻辑版本。命中缓存时，第 72–75 行仍读取当前第一份文件的 `info`。

   **影响：**增加被试、替换预处理文件、修改事件或通道后，仍可能返回旧数组；甚至出现旧数组与新 montage 配对。现有缓存的样本数与规格一致，但这不能证明内容仍然有效。

   **具体修复：**缓存保存输入清单及指纹、被试 ID、通道顺序、时间轴和加载器版本；命中前验证。最低限度可用路径、大小和高精度修改时间失效，正式归档再保留内容哈希。缓存中的数据与元信息必须来自同一次验证。缺少这些信息的旧缓存应重建。

3. **P0｜颜色数量不足会静默丢掉整组数据。**

   **证据：**[erp_plot.py:143](C:/dev/brain-plot/erp-figure/erp_plot.py:143) 的组色板有 8 项，但黑色重复，只有 7 种独特色；类别条件色板最多 7 项。第 253、268 行通过 `zip(..., colors)` 绘图。内存检查确认：**9 组输入只画出 8 条线、8 幅头皮图，也只列出 8 组图例。**自定义颜色列表过短同样如此。

   **影响：**这是数据遗漏，不只是配色问题。

   **具体修复：**绘图前强制验证颜色覆盖全部线条，并在绘图后断言“预期线条数＝实际线条数＝头皮图数”。第一版超过可辨认上限时明确拒绝即可，无需无限扩展色板。颜色应绑定组／条件 ID；“第一组黑色就是参考组”必须来自确认规格，不能来自目录顺序。

4. **P0｜旧 SKILL 与代码之间存在会改变科学含义的接口分歧。**

   **证据：**[SKILL.md:51](C:/dev/brain-plot/erp-figure/SKILL.md:51) 示例把电极放在顶层 `channels`，而 [erp_plot.py:229](C:/dev/brain-plot/erp-figure/erp_plot.py:229) 要求每个 component 自带 `channels`。文档承诺的 `difference`、`difference_waveform`、`kind`、`error` 没有相应绘图实现。对 `-ave.fif`，`query` 被忽略，但 [erp_plot.py:303](C:/dev/brain-plot/erp-figure/erp_plot.py:303) 仍可能把它写成已执行的试次筛选。

   **影响：**agent 可以以为画的是差异波、关闭了 SEM、筛选了正确试次，实际输出却不是如此。这里不能只靠“之后更新文案”解决。

   **具体修复：**先冻结一套最小 spec 契约：必需字段、允许值、支持的输入和图型；拒绝未知或不支持的科学参数。`Evoked + query` 应直接报错。差异波可以暂缓实现，但必须明确不支持。唯一示例规格必须能直接通过验证并运行。

5. **P1｜排除、样本身份和不确定性定义不足以支持跨研究复用。**

   **证据：**[erp_plot.py:67](C:/dev/brain-plot/erp-figure/erp_plot.py:67) 用文件名前缀排除；第 38–40 行按文件计被试，且存在组目录时忽略根目录文件；第 236 行在 `n=1` 时把 SEM 设为零。[SKILL.md:26](C:/dev/brain-plot/erp-figure/SKILL.md:26) 还推荐默认保留 `n≥10` 的组。

   **影响：**前缀可能误排其他 ID；同一人的 Epochs 和 Evoked 文件可能被重复计数；零 SEM 会让单被试看似没有不确定性。按组大小建议删除组，也不应成为绘图工具的默认科学决策。

   **具体修复：**使用明确被试 ID，并输出纳入／排除清单、原因、未匹配排除项及重复 ID。保留所有符合已确认分析标准的组，低样本量只提示。`n<2` 时不画 SEM，并明确其不可估计。将目前误差带定义为“逐时点、被试间 ±SEM”；它不是条件差异或交互作用的检验。未来差异波必须先在匹配被试内相减，再计算差异的 SEM。

   当前 31／23／20／19 与文件清单一致；抽查文件的 Accuracy 仅见 Hit／CR，因此**没有依据要求当前数据再盲目加一次正确试次筛选**。

6. **P1｜窗口推荐把一个启发式方法描述得过于确定。**

   **证据：**[erp_plot.py:124](C:/dev/brain-plot/erp-figure/erp_plot.py:124) 将所有被试与条件合并，在全部正时间点寻找峰，以全程振幅范围的 10% 作为 prominence 阈值，再输出 `peak_widths(rel_height=0.5)`。这实际是**半 prominence 高度处的全峰宽区间**，不是通用的“峰值±半宽”规则。[SciPy 官方说明](https://docs.scipy.org/doc/scipy/reference/generated/scipy.signal.peak_widths.html)

   **影响：**它可能推荐其他成分、晚期反应或刺激消失后的峰，不能只凭极性和潜伏期自动命名。代码和旧 SKILL 所称“orthogonal to effects”也不能无条件保证统计独立；合并权重、方差结构及目标比较都有关。组人数不等本身则不足以判定当前方法无效。

   **具体修复：**先确认成分、ROI、搜索区间和窗口宽度策略，再产生候选；记录合并权重、搜索范围和来源文件。保留“不根据当前待检验差异挑窗口”的原则，但不要把所有独立数据上的差异定位都一概禁止。[Luck 与 Gaspelin 的方法讨论](https://pmc.ncbi.nlm.nih.gov/articles/PMC5178877/)

   当前确认窗口与外部 `erp_windows.json` 一致，**不建议因本审查重新择窗**。另外，500 Hz 下 N2 的实际采样点为 306–354 ms，应在运行记录中保留请求窗口与实际采样边界。旧 SKILL 第 39 行的“相邻成分窗口不得重叠”也不应成为普遍科学规则。

7. **P1｜用户规则仍有冲突，agent 无法获得唯一解释。**

   **证据：**[exemplars.md:30](C:/dev/brain-plot/research/exemplars.md:30) 及后续规则存在以下分歧：

   | 位置 | 冲突 | 应采用的整理方式 |
   |---|---|---|
   | 规则 5 与 10 | 头皮图在下方／右侧 | 保留规则 5 的单成分和一线一图；位置由后续规则 10 更新为右侧 |
   | 规则 7 与 9 | 条件名在顶部／底部 | 保留电极标题在上；条件名按规则 9 放下方 |
   | 自适应表与规则 10 | 每组一行／每条件一行 | 当前确认版采用条件分面、组别叠加；其他比较方向必须进入规格 |
   | 规则 8 与代码 | 检查右侧 1/3／检查图例实际宽度 | 按绑定规则检查右侧 1/3，再验证图例实际边界是否相交 |
   | 默认表与自适应表 | 类别统一 Okabe–Ito／两条件专用双色 | 明确配色作用于“叠加变量”，并固定映射 |

   **影响：**同一 agent 前后两次都可能作出不同但“有文档依据”的决定。

   **具体修复：**形成一份带版本的有效规则表，每条只有一个现行表述，标明“用户要求／方法要求／默认设计选择”。上述绑定规则应保留；不要包装为所有 ERP 论文的普遍规范。规则 6 也应区分显示区间与分析窗口：刺激消失引起的反应不天然等于伪迹，不能因其不好看而自动裁掉。

8. **P1｜共享色标做对了，但头皮图的其他比较条件尚未固定。**

   **证据：**[erp_plot.py:242](C:/dev/brain-plot/erp-figure/erp_plot.py:242) 正确计算图内共同对称范围；但第 272 行使用 `contours=4`，未固定等值线数值、sphere、插值和外推策略。整数 contours 会为每幅图自动选择等值线级别，不能保证同一条线代表同一电位。[MNE 官方说明](https://mne.tools/stable/generated/mne.viz.plot_topomap.html)

   **影响：**读者可能把不同等值线密度理解为空间分布差异。当前头圈之外的着色不自动意味着计算错误，但需要明确投影和外推约定；还应检查插值值是否超出按传感器值确定的色标范围。

   **具体修复：**图内显式共享同一组等值线数值；固定并记录投影、插值、外推参数与软件版本，检查饱和区域。电位、CSD、统计量必须分清，第一版可直接拒绝后两者。E4 的 CSD 图可以启发布局，不能为电位图的单位或科学计算背书。

   N2 的 ±41.5 µV 与 P3 的 ±21.4 µV **本身不是错误**：当前规则是每个成分独立成图、图内共享。只有声明跨成分比较绝对振幅时，才需要另行约定共同范围。

9. **P1｜“grilling → 确认 → 绘图 → QA”方向正确，但必须落实为可检查的交付契约。**

   **证据：**[SKILL.md:20](C:/dev/brain-plot/erp-figure/SKILL.md:20) 仍是一次性参数问卷；第 48 行确认没有绑定具体规格版本；第 76 行 QA 主要依赖 agent 看图。当前 spec 也没有保存图的主张、关键比较、锁时事件和误差带含义。

   **影响：**参数齐全不代表图意正确；视觉 QA 更无法发现旧缓存、丢组或时间轴错位。

   **具体修复：**采用计划中的分轮访谈，每轮少量问题，推荐答案附理由。科学信息无法从数据确定时，推荐“待确认”，不能为让用户回复“ok”而补值。对本研究，应把“描述四组 ERP 与地形；据用户提供的结果，交互未通过 FDR；不主张等效”写入契约。

   | 层次 | 应承担的责任 |
   |---|---|
   | `SKILL.md` | 访谈顺序、科学边界、如何呈现推荐、显式确认、何时重新确认、交付要求 |
   | 代码 | schema 验证、样本与时间轴检查、计算、固定样式、完整性断言、尺寸与可测量 QA |
   | `references/` | 有效规则及来源、窗口与误差带解释、正反例、人工视觉 QA 清单 |
   | 每次运行记录 | 已确认规格、输入清单、实际参数、版本、图注事实和 QA 结果 |

   确认应绑定具体规格内容；科学内容变更才重新确认，已授权样式规则内的排版修复可以继续迭代。代码不能证明“人确实同意”，但必须拒绝无效规格。跨 agent 复用还需去掉 [SKILL.md:9](C:/dev/brain-plot/erp-figure/SKILL.md:9) 的个人环境依赖，提供最小依赖清单和已验证版本；当前环境使用的是 MNE 开发版。

10. **P1｜还有几项小而真实的边界错误，应该集中修复，不必重构成包。**

   **证据、影响与最小修复：**

   | 问题 | 证据 | 最小修复 |
   |---|---|---|
   | 显示上限低于 200 ms 即崩溃 | [erp_plot.py:169](C:/dev/brain-plot/erp-figure/erp_plot.py:169)，空 `xt` 后访问 `xt[-1]`；100、150 ms 均已复现 | 根据显示范围生成刻度，支持早期成分及反应锁时负时间 |
   | 空组、空窗口、越界窗口无预检 | 第 71、213、230 行 | 验证非空、顺序及数据覆盖；不允许偷偷只平均窗口的一部分 |
   | 非有限值、全零数据没有明确处理 | 第 238–242 行 | 非有限值报错；全零数据使用明确的非退化坐标范围 |
   | 图例判断区间违背规则 8 | 第 188 行 | 固定右侧 1/3，使用均值±SEM 包络；扩轴后再检查实际碰撞 |
   | 时间刻度避让只看各线均值 | 第 257–258 行 | 使用完整包络／实际文字边界，避免平均后掩盖单条线的遮挡 |

   单组、三条件以上、负向朝上、长标签和短时间范围都应有小型回归样例。差异波、侧化成分、复杂因子设计可以明确列为未支持，不能让现有普通 ROI 平均自动冒充这些分析。代码没有必要拆成大型框架；过时的 `plot()` 布局说明和未使用的 `pr` 只是低优先级清理项。

11. **P1/P2｜当前 PNG 已有统一风格，但缺少独立阅读所需的信息，物理尺寸也不符合规格。**

   **证据：**[N2 图](C:/dev/brain-plot/erp-figure/test/out/fig_N2.png)、[P3 图](C:/dev/brain-plot/erp-figure/test/out/fig_P3.png) 均未在图中写出具体时间窗；刺激前没有时间刻度，原点也没有数字标签。规则 1 明确要求标出窗口。[erp_plot.py:290](C:/dev/brain-plot/erp-figure/erp_plot.py:290) 的 `bbox_inches="tight"` 则改变了最终尺寸。

   **实测：**两张 PNG 均为 3698×2344、标称 600 dpi；SVG 宽为 443.73 pt，即 **156.54 mm，而不是 180 mm**。若排版时放大至 180 mm，色标字约 6.3 pt，图例／刻度约 6.9 pt，电极标题约 8.0 pt。因此不能笼统说当前字体不可读，但“固定 7 pt、固定 180 mm”的保证没有兑现。

   **影响：**读者仅凭图不能知道灰带和头皮图的精确窗口；不同输出的裁边还会改变论文中的字号与线宽。

   **具体修复：**保留现有布局，在头皮图区或成分标签处加入 `305–355 ms`／`522–622 ms`，补足 −200 与 0 ms 参照，图注明确锁时事件。使用固定最终画布和显式边距，导出后验证真实宽度。当前 a/c 与 b/d 的垂直位置不一致、行间留白偏多、头轮廓相对波形略重，可统一调整；现有图例位置清楚、没有明显压住主波形，无需改到图外。

   [当前图注](C:/dev/brain-plot/erp-figure/test/out/fig_N2_caption.md:3) 仍是事实清单，还需补基线、锁时事件、排除依据，以及“白点是 ROI，不是显著性标记”。P3 的窗口来源不能单独写 “same file”。关于 FDR 的描述应来自已提供的统计结果，不能从 SEM 重叠或图形形态推断“无差异”。

12. **P2｜精简 exemplar 路线合理，但图库不能直接充当方法学证据库。**

   **证据：**[exemplars.md:5](C:/dev/brain-plot/research/exemplars.md:5) 正式列出 E1–E4，另有 E5 用户图；图库还混有时频／功率图、综述图和效应量图，例如 [erp-gallery.html:23](C:/dev/brain-plot/research/erp-gallery.html:23) 与第 200 行。这些可以作为候选，但不能一起推导电位图规则。原始计划仍显示“正式检索尚未开始”，没有标明已被精简路线替代。

   **影响：**后续 agent 可能重新启动大规模调查，或把一次性审美选择误写成领域规范。

   **具体修复：**把原计划标为归档；保留一个小型 exemplar 清单，记录实际看过的图、数据类型、采用的设计点及不采用的部分。约 20 幅应追求设计覆盖，不必凑数量。另有一个可独立修复的图库 bug：[erp-gallery.html:207](C:/dev/brain-plot/research/erp-gallery.html:207) 的 `.join('…')` 字符串中含真实换行，会导致整段 JavaScript 解析失败；应改为转义换行。

**must fix before writing the skill**

1. 冻结最小输入／spec 契约，落实时间轴、通道、单位、样本身份、排除和不支持参数的验证。
2. 修复缓存失效与数据／元信息绑定，保留输入清单及处理版本。
3. 消除静默丢组、错误 SEM 和关键边界错误，增加少量能验证科学结果的合成样例。
4. 合并为唯一有效规则库，确定窗口推荐边界及“访谈—规格确认—QA”的职责划分。
5. 将当前两图验收为真正的 180 mm 输出，补齐窗口、时间参照、图注事实及自动完整性检查。

**can wait**

- 恢复 200 篇综述、扩充到大量 exemplar、为审美选择统计频率。
- 完整 Python 包、多主题、更多色图和各 agent 的专用封装。
- 差异波、侧化 ERP、CSD、复杂设计的实现；第一版明确拒绝即可。
- 图库交互修复、细小留白与轮廓线优化、无关死代码清理。