# 项目规则

## 语言（硬规则）

**所有给用户看的文字一律用中文。** 包括：最终回复、工具调用之间的一句话进度说明、汇总报告、选项、提问。

- 只有技能名、文件名、路径、代码、命令、原样粘贴的工具输出和学术术语保持英文原样。
- 工具输出、代码、`SKILL.md`、`rules.md` 都是英文，最容易在这时顺着切回英文——长串工具调用之后写汇报前，先检查语言。
- 发出任何文字前自查：这段是不是中文？不是就重写。
- 给 agent 读的文件（代码、注释、`SKILL.md`、`references/`、日志、commit message）保持英文，不受此条影响。
